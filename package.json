{
  "name": "peptra",
  "version": "0.1.0",
  "private": true,
  "description": "Peptra — landing page and waitlist for a research peptide supplier.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test test/*.test.js"
  }
}
