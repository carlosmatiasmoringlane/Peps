{
  "name": "peakline",
  "version": "0.2.0",
  "private": true,
  "description": "Peakline — static site for a research peptide supplier.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npx --yes serve public",
    "test": "node --test test/*.test.js",
    "build": "node tools/build-site.mjs"
  }
}
